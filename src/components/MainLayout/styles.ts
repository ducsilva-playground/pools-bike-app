import { StyleSheet } from 'react-native';

import { Colors } from 'theme';

export default StyleSheet.create({
  avatar: {
    width: 40,
    height: 40,
    backgroundColor: Colors.colorB1,
    borderRadius: 50,
  },
  notiNew: {
    width: 8,
    height: 8,
    borderRadius: 8,
    backgroundColor: Colors.colorFF00,
    position: 'absolute',
    left: 15,
  },
});
